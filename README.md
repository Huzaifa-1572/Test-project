# React + Vite

This template provides a minimal setup to get React working in Vite with HMR and some ESLint rules.

Currently, two official plugins are available:

- [@vitejs/plugin-react](https://github.com/vitejs/vite-plugin-react/blob/main/packages/plugin-react/README.md) uses [Babel](https://babeljs.io/) for Fast Refresh
- [@vitejs/plugin-react-swc](https://github.com/vitejs/vite-plugin-react-swc) uses [SWC](https://swc.rs/) for Fast Refresh


                            =============Task============
                            
1 Identify the code duplication issue in the components. Why is this a problem? and How would you remove this duplication?

2 Make responsive and improve performance for face detection and eye blinking model

3 Add a progress indicator showing the current step out of 2.

4 Improve the camera permission error message with clear guidance for users.

5 Add a visual face positioning guide overlay on the webcam.

6 Introduce a 3-second countdown before capturing the final photo.

7 Display a loading state while models are being initialized.

8 Show text captions matching the audio instructions.

9 Validate image brightness before accepting the captured photo.

10 Add debugging logs to monitor detection progress and system behavior.